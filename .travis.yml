language: python

python:
  - "3.6"
  - "3.7"
  - "3.8"

dist: xenial

services:
  - xvfb

before_script:
  - "export DISPLAY=:99.0"
  - "sh -e /etc/init.d/xvfb start"
  - sleep 3

install:
  - pip install .

script:
  - python -m unittest discover -v test
